# Keep this repo's labels in sync with the canonical set in the spec (OPS-R12).
name: labels
on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * 1"
permissions:
  contents: read
jobs:
  sync:
    permissions:
      contents: read
      issues: write
    uses: lemonfiber/spec/.github/workflows/label-sync.yml@d37cfa7e30b997f1e74a9713bed127cc05bf497e # main
