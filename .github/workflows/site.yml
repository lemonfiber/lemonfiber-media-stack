# The website mirrors this repository — its implementation status, its releases —
# so it is told when they change rather than waiting for its daily schedule.
name: site

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rebuild:
    permissions:
      contents: read
    uses: lemonfiber/spec/.github/workflows/rebuild-site.yml@fa446d8dbc63dde506b688511b612f83ef448ac5 # v1.0.8
    with:
      because: "the stack changed"
    secrets:
      # Only what the dispatch needs: the App key it mints a website-scoped token with.
      RELEASE_APP_KEY: ${{ secrets.RELEASE_APP_KEY }}
